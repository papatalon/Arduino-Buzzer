#ifndef PHASEMODE_H
#define PHASEMODE_H

// Define the PhaseMode enum here
enum PhaseMode {
    CONFIGURATION,
    SHUFFLE_BUZZER,
    BUZZER_CONFIG,
    RESET,
    INTRO,
    WAITING_BUZZER,
    BUZZER_PRESSED,
    SHOW_SCORES,
    END_CONFIRM,
    END_GAME,
    VOLUME
};

#endif